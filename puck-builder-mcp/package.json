{
  "name": "puck-builder-mcp",
  "version": "7.0.0",
  "description": "Ω⁷ Puck Builder MCP — AI-universal page generation server. 40 blocks, 7 tools, 6 presets, multi-model AI adapters (OpenAI, Anthropic, Ollama), streaming, telemetry.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "puck-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
